---
title: Clone between cards
description: Create a second encrypted SeedKeeper backup
---

# Clone between cards

Initialize card B separately, then clone the encrypted record from card A in an offline workspace. Check both card identifiers before and after the operation.

The second card should be stored in a different secure location. Cloning is not a reason to reveal the plaintext seed.

